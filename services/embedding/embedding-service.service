[Unit]
Description=Embedding gRPC Service
After=network.target

[Service]
Type=simple
User=embedding
WorkingDirectory=/opt/embedding-service
ExecStart=/usr/bin/python3 /opt/embedding-service/embedding_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
